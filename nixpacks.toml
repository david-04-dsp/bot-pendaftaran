[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.sqlite3", "php82Extensions.mbstring"]

[phases.install]
cmds = ["echo 'PHP dependencies ready'"]

[start]
cmd = "bash start_railway.sh"
